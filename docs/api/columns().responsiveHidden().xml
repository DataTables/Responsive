<?xml version="1.0" encoding="UTF-8" ?>
<dt-api library="Responsive">
	<name>columns().responsiveHidden()</name>
	<summary>Get the responsive visibility state of columns in the table</summary>
	<since>2.2.1</since>

	<type type="function">
		<signature>columns().responsiveHidden()</signature>
		<description>For the selected columns, determine if Responsive has hidden them or not.</description>
		<returns type="DataTables.Api">API instance that contains boolean values in the result set - `true` if the column is responsive visible, `false` otherwise.</returns>
	</type>

	<description>
		This method provides the ability to determine the Responsive visibility state of columns in a table.

		Please note that this is not the same as the column visibility control built into DataTables (`-api columns().visible()` / `-init columns.visible`) - Responsive implements its own column hiding.

	</description>

    <related>-api responsive.hasHidden()</related>
    <related>-api column().responsiveHidden()</related>
    <related>-api columns().visible()</related>
</dt-api>
